const Messages = () => {
  console.log("logges");
  return (
    // <div>
    //   {!selectedPerson ? (
    //     <div className="h-full text-gray-700  text-xl font-sans font-bold text-center">
    //       No users are selected
    //     </div>
    //   ) : (
    //     <div>
    //       {message.forEach((m) => (
    //         <div>{m}</div>
    //       ))}
    //     </div>
    //   )}
    // </div>
    <div>hello</div>
  );
};

export default Messages;
